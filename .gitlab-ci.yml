image: python:latest
stages:
  - build

build:
  stage: build
  script:
    - pip install -r requirements.txt 
    - make html
    - chmod 400 $SSH_RSA
    - rsync --delete -Pav -e "ssh -i $SSH_RSA" ./build/html/ root@www.mathcrowd.cn:/var/www/sphinx
  artifacts:
    paths:
      - build